interface:
  display_name: "Apex Init Project Code"
  short_description: "Find and write missing code headers"
  default_prompt: "Use apex-init-project-code to find code files missing standard headers, then write accurate context-aware comments without overwriting existing headers."
