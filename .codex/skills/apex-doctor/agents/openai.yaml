interface:
  display_name: "Apex Doctor"
  short_description: "检查 ApexPowers 安装、agent mirrors、hooks manifest 和 workflow state 健康状态"
  default_prompt: "使用 apex-doctor 检查当前项目的 ApexPowers 安装健康状态；默认只读检查，需要机器可读输出时加 --json。"
